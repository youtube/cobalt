services:
  linux:
    build:
      context: cobalt/docker/linux
    image: "${DOCKER_IMAGE_PREFIX:-ghcr.io/youtube/cobalt}/linux:${DOCKER_BRANCH_TAG:-main}"
    platform: linux/amd64
  raspi:
    build:
      context: cobalt/docker/raspi
    image: "${DOCKER_IMAGE_PREFIX:-ghcr.io/youtube/cobalt}/raspi:${DOCKER_BRANCH_TAG:-main}"
    platform: linux/amd64
  unittest:
    build:
      context: cobalt/docker/unittest
    image: "${DOCKER_IMAGE_PREFIX:-ghcr.io/youtube/cobalt}/unittest:${DOCKER_BRANCH_TAG:-main}"
  webtest:
    build:
      context: cobalt/docker/webtest
    image: "${DOCKER_IMAGE_PREFIX:-ghcr.io/youtube/cobalt}/webtest:${DOCKER_BRANCH_TAG:-main}"
