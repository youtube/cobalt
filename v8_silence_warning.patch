diff --git a/third_party/v8/BUILD.gn b/third_party/v8/BUILD.gn
index 90499f8261..30425f2c67 100644
--- a/third_party/v8/BUILD.gn
+++ b/third_party/v8/BUILD.gn
@@ -977,6 +977,8 @@ config("toolchain") {
       cflags += [
         # bit-field.h integer value is outside the valid range of values
         "-Wno-enum-constexpr-conversion",
+        # warning: implicit conversion from 'long' to 'double' changes value.
+        "-Wimplicit-const-int-float-conversion",
       ]
     }
     if (v8_current_cpu != "mips" && v8_current_cpu != "mipsel") {
