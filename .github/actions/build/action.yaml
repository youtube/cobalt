name: Build Cobalt
description: Builds Cobalt targets
runs:
  using: "composite"
  steps:
    - name: Set common env vars
      run: |
           echo "PYTHONPATH=$GITHUB_WORKSPACE" >> $GITHUB_ENV
      shell: bash
    - name: Set Android env vars
      if: startsWith(${{matrix.target_platform}}, 'android')
      run: |
        echo "ANDROID_HOME=/__w/starboard-toolchains/AndroidSdk/" >> $GITHUB_ENV
        echo "COBALT_GRADLE_BUILD_COUNT=4" >> $GITHUB_ENV
        echo "GCS_NIGHTLY_PATH=gs://cobalt-actions-devel-build-artifacts" >> $GITHUB_ENV
        cp -R /root/starboard-toolchains /__w/
      shell: bash
    - name: Print env vars
      run: env | sort
      shell: bash
    - name: Build ${{matrix.platform}}-${{matrix.config}}
      run: |
           set -x
           ln -s /__w/starboard-toolchains /github/home/starboard-toolchains
           target=all
           if [[ "${{ matrix.config }}" =~ ^(qa|gold)$ ]]; then
             target=default
           fi
           ninja -v -C ${GITHUB_WORKSPACE}/out/${{ matrix.target_platform }}_${{ matrix.config }} ${target}
      shell: bash
