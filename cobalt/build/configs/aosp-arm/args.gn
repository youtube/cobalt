import("//cobalt/build/configs/evergreen-x64/args.gn")

target_cpu = "arm"
starboard_target_platform = "aosp-arm"

# AOSP uses the Android Starboard media implementation
# See use_android_media in starboard/build/buildflags.gni.
use_android_media = true

arm_float_abi = "softfp"
use_errorprone_java_compiler = false
