--- ui/gl/gl_fence.cc
+++ ui/gl/gl_fence.cc
@@ -17,16 +17,7 @@
 #include "ui/gl/gl_surface_egl.h"
 #include "ui/gl/gl_version_info.h"
 
-<<<<<<< HEAD
-#if BUILDFLAG(IS_ANDROID) || BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_LINUX)
-=======
-#if BUILDFLAG(IS_APPLE)
-#include "ui/gl/gl_fence_apple.h"
-#endif
-
-#if defined(USE_EGL)
-#if BUILDFLAG(IS_POSIX) && !BUILDFLAG(IS_APPLE) && !BUILDFLAG(IS_STARBOARD)
->>>>>>> fc296807c1f ([POSIX] Remove references to ioctl (#6641))
+#if BUILDFLAG(IS_ANDROID) || BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_LINUX) && !BUILDFLAG(IS_STARBOARD)
 #define USE_GL_FENCE_ANDROID_NATIVE_FENCE_SYNC
 #include "ui/gl/gl_fence_android_native_fence_sync.h"
 #endif
