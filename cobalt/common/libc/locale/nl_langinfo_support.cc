// Copyright 2025 The Cobalt Authors. All Rights Reserved.
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License-for-dev at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#include "cobalt/common/libc/locale/nl_langinfo_support.h"

#include <algorithm>
#include <array>
#include <string>
#include <utility>

#include "starboard/common/log.h"
#include "third_party/icu/source/common/unicode/localebuilder.h"
#include "third_party/icu/source/common/unicode/locid.h"
#include "third_party/icu/source/common/unicode/uscript.h"
#include "third_party/icu/source/i18n/unicode/dcfmtsym.h"
#include "third_party/icu/source/i18n/unicode/dtfmtsym.h"
#include "third_party/icu/source/i18n/unicode/dtptngen.h"

namespace cobalt {
namespace {

constexpr UChar kSpace = ' ';
constexpr UChar kNonBreakingSpace = 0x00A0;
constexpr UChar kNarrowNonBreakingSpace = 0x202F;

// 0x80 is the upper limit for standard 7-bit ASCII.
constexpr UChar32 kAsciiLimit = 0x80;

// String containing the supported ICU tokens for date/time formatting strings.
// When we try to translate an ICU token to a POSIX equivalent token, we check
// this string to see if the current token we are examining is a formatted ICU
// character that we can convert.
constexpr std::string_view kFormatChars = "yMdELGHhKkmaszv";

// The skeleton strings used to receive an ICU format string.
//
// Using these skeleton strings allow us to use a broad, generalized approach in
// constructing usable and readable format patterns for locales. ICU will
// automatically take into account locale subtleties (how time is displayed in
// the locale, the order of the information displayed), and generate an ICU
// pattern that is information complete and follows any rules generated by the
// locale.
constexpr char kSkeletonDtFmt[] = "yMMMEdjmsz";
constexpr char kSkeletonTFmtAmPm[] = "ahms";

// Convenience method to convert icu::UnicodeStrings to std::string.
std::string ToUtf8(const icu::UnicodeString& us) {
  std::string out;
  us.toUTF8String(out);
  return out;
}

// Helper method to remove redundant and unnecesssary spaces in POSIX
// formatting strings that were converted from ICU. Since our converter
// does not support specific ICU symbols, there are instances where
// there are extra gaps in the converted string.
std::string CollapseSpaces(const std::string& input) {
  std::string result;
  result.reserve(input.length());

  bool lastWasSpace = false;
  for (char c : input) {
    if (c == kSpace) {
      if (!lastWasSpace) {
        result += kSpace;
        lastWasSpace = true;
      }
    } else {
      result += c;
      lastWasSpace = false;
    }
  }

  if (!result.empty() && result.back() == kSpace) {
    result.pop_back();
  }

  return result;
}

// Returns the relevant LC_TIME nl_langinfo string from the C/POSIX locale.
std::string GetCTimeLanginfo(nl_item item) {
  switch (item) {
    // Date and Time Formats
    case D_T_FMT:
      return "%a %b %e %H:%M:%S %Y";
    case D_FMT:
      return "%m/%d/%y";
    case T_FMT:
      return "%H:%M:%S";
    case T_FMT_AMPM:
      return "%I:%M:%S %p";
    case AM_STR:
      return "AM";
    case PM_STR:
      return "PM";

    // Days of the Week (Full)
    case DAY_1:
      return "Sunday";
    case DAY_2:
      return "Monday";
    case DAY_3:
      return "Tuesday";
    case DAY_4:
      return "Wednesday";
    case DAY_5:
      return "Thursday";
    case DAY_6:
      return "Friday";
    case DAY_7:
      return "Saturday";

    // Days of the Week (Abbreviated)
    case ABDAY_1:
      return "Sun";
    case ABDAY_2:
      return "Mon";
    case ABDAY_3:
      return "Tue";
    case ABDAY_4:
      return "Wed";
    case ABDAY_5:
      return "Thu";
    case ABDAY_6:
      return "Fri";
    case ABDAY_7:
      return "Sat";

    // Months (Full)
    case MON_1:
      return "January";
    case MON_2:
      return "February";
    case MON_3:
      return "March";
    case MON_4:
      return "April";
    case MON_5:
      return "May";
    case MON_6:
      return "June";
    case MON_7:
      return "July";
    case MON_8:
      return "August";
    case MON_9:
      return "September";
    case MON_10:
      return "October";
    case MON_11:
      return "November";
    case MON_12:
      return "December";

    // Months (Abbreviated)
    case ABMON_1:
      return "Jan";
    case ABMON_2:
      return "Feb";
    case ABMON_3:
      return "Mar";
    case ABMON_4:
      return "Apr";
    case ABMON_5:
      return "May";
    case ABMON_6:
      return "Jun";
    case ABMON_7:
      return "Jul";
    case ABMON_8:
      return "Aug";
    case ABMON_9:
      return "Sep";
    case ABMON_10:
      return "Oct";
    case ABMON_11:
      return "Nov";
    case ABMON_12:
      return "Dec";

    default:
      return "";
  }
}

// Returns the relevant LC_NUMERIC nl_langinfo string from the C/POSIX locale.
std::string GetCNumericLanginfo(nl_item item) {
  switch (item) {
    case RADIXCHAR:
      return ".";
    case THOUSEP:
    default:
      return "";
  }
}

// Convenience method to convert a POSIX locale string to the ICU string format.
// Some POSIX strings like sr_RS@latin require special handling to be fully
// translated from POSIX to ICU.
icu::Locale GetCorrectICULocale(const std::string& posix_name) {
  UErrorCode status = U_ZERO_ERROR;

  char canonical_buffer[ULOC_FULLNAME_CAPACITY];
  uloc_canonicalize(posix_name.c_str(), canonical_buffer,
                    ULOC_FULLNAME_CAPACITY, &status);

  if (U_FAILURE(status)) {
    return icu::Locale::createCanonical(posix_name.c_str());
  }

  icu::Locale loc = icu::Locale::createFromName(canonical_buffer);
  const char* variant = loc.getVariant();

  // ICU will store the modifier variable (e.g. @latin) in its variant field, if
  // it exists. For ICU to correctly translate the string, we must convert the
  // script to its shortname and set it in the script field.
  if (variant && *variant) {
    UScriptCode codes[1];

    UErrorCode script_status = U_ZERO_ERROR;
    int32_t num_codes = uscript_getCode(variant, codes, 1, &script_status);

    if (U_SUCCESS(script_status) && num_codes == 1) {
      UScriptCode actual_script = codes[0];

      if (actual_script != USCRIPT_INVALID_CODE &&
          actual_script != USCRIPT_UNKNOWN) {
        const char* short_name = uscript_getShortName(actual_script);

        if (short_name) {
          icu::LocaleBuilder builder;
          builder.setLanguage(loc.getLanguage());
          builder.setRegion(loc.getCountry());
          builder.setScript(short_name);

          return builder.build(status);
        }
      }
    }
  }

  return loc;
}

// ICU and POSIX use different systems to represent date format strings, and
// MapIcuTokenToPosix will make a best attempt to convert an ICU token to its
// POSIX equivalent.
std::string MapIcuTokenToPosix(const icu::UnicodeString& token) {
  constexpr std::array<std::pair<std::string_view, const char*>, 46>
      kIcuToPosixMapping = {
          {// Date
           {"E", "%a"},
           {"EE", "%a"},
           {"EEE", "%a"},
           {"EEEE", "%A"},
           {"M", "%m"},
           {"MM", "%m"},
           {"MMM", "%b"},
           {"MMMM", "%B"},
           {"d", "%d"},
           {"dd", "%d"},
           {"y", "%Y"},
           {"yy", "%y"},
           {"yyyy", "%Y"},
           // Time
           {"H", "%k"},
           {"HH", "%H"},
           {"K", "%l"},
           {"KK", "%I"},
           {"h", "%l"},
           {"hh", "%I"},
           {"k", "%H"},
           {"kk", "%H"},
           {"m", "%M"},
           {"mm", "%M"},
           {"s", "%S"},
           {"ss", "%S"},
           // AM/PM (all variants map to the same POSIX token)
           {"a", "%p"},
           {"aa", "%p"},
           {"aaa", "%p"},
           {"aaaa", "%p"},
           // Era (G) (Currently not supported, will return the empty string if
           // seen)
           {"G", ""},
           {"GG", ""},
           {"GGG", ""},
           {"GGGG", ""},
           {"GGGGG", ""},
           // ICU Timezone specific symbols (POSIX has no direct equivalent for
           // these symbols, so we simply just return "%Z", which is the
           // timezone name/abbreviation)
           {"V", "%Z"},
           {"VV", "%Z"},
           {"VVV", "%Z"},
           {"VVVV", "%Z"},
           {"v", "%Z"},
           {"vv", "%Z"},
           {"vvv", "%Z"},
           {"vvvv", "%Z"},
           {"z", "%Z"},
           {"zz", "%Z"},
           {"zzz", "%Z"},
           {"zzzz", "%Z"}}};

  std::string token_str = ToUtf8(token);
  for (const auto& entry : kIcuToPosixMapping) {
    if (entry.first == token_str) {
      return entry.second;
    }
  }

  return token_str;
}

// Helper to process the current token and append it to the result.
void FlushToken(icu::UnicodeString& currentToken, std::string& result) {
  if (!currentToken.isEmpty()) {
    result += MapIcuTokenToPosix(currentToken);
    currentToken.remove();
  }
}

// IcuPatternToPosix will iterate through an ICU format pattern, translating any
// ICU-specific tokens to a readable POSIX format.
std::string IcuPatternToPosix(const icu::UnicodeString& pattern) {
  std::string result;
  bool inQuote = false;
  icu::UnicodeString currentToken;

  for (int32_t i = 0; i < pattern.length(); ++i) {
    const UChar c = pattern[i];
    if (c == '\'') {
      if (i + 1 < pattern.length() && pattern[i + 1] == '\'') {
        FlushToken(currentToken, result);
        result += "'";
        i++;
      } else {
        inQuote = !inQuote;
      }
    } else if (inQuote) {
      FlushToken(currentToken, result);
      result += ToUtf8(icu::UnicodeString(c));
    } else if (kFormatChars.find(c) != std::string::npos) {
      if (currentToken.isEmpty() || currentToken[0] == c) {
        currentToken.append(c);
      } else {
        FlushToken(currentToken, result);
        currentToken.append(c);
      }
    } else {
      FlushToken(currentToken, result);

      // ICU patterns for some locales may include a space after a colon in time
      // formats (e.g., "H: mm"), which is not typical in POSIX. This removes
      // that space to improve compatibility.
      if (c == kSpace && !result.empty() && result.back() == ':') {
        continue;
      }
      if (c == kNonBreakingSpace || c == kNarrowNonBreakingSpace) {
        result += kSpace;
      } else {
        if (c < kAsciiLimit) {
          result += static_cast<char>(c);
        } else {
          result += ToUtf8(icu::UnicodeString(c));
        }
      }
    }
  }

  FlushToken(currentToken, result);
  return result;
}

// Helper method to retrieve the requested ICU pattern using udat_toPattern.
icu::UnicodeString GetPatternFromStyle(const std::string& locale_id,
                                       UDateFormatStyle date_style,
                                       UDateFormatStyle time_style) {
  UErrorCode status = U_ZERO_ERROR;

  UDateFormat* fmt = udat_open(time_style, date_style, locale_id.c_str(),
                               nullptr, 0, nullptr, 0, &status);

  if (U_FAILURE(status)) {
    SB_LOG(WARNING)
        << "Unable to obtain an ICU pattern for GetPatternFromStyle. "
           "GetPatternFromStyle will return the empty string.";
    return icu::UnicodeString();
  }

  constexpr int32_t kStackBufferSize = 128;
  UChar stack_buffer[kStackBufferSize];

  int32_t len =
      udat_toPattern(fmt, false, stack_buffer, kStackBufferSize, &status);

  icu::UnicodeString result;

  if (status == U_BUFFER_OVERFLOW_ERROR) {
    status = U_ZERO_ERROR;
    std::vector<UChar> heap_buffer(len + 1);
    udat_toPattern(fmt, false, heap_buffer.data(), len + 1, &status);

    if (U_SUCCESS(status)) {
      result = icu::UnicodeString(heap_buffer.data(), len);
    }
  } else if (U_SUCCESS(status)) {
    result = icu::UnicodeString(stack_buffer, len);
  }

  udat_close(fmt);

  return result;
}

// Given a locale and a format skeleton string, GetPatternFromSkeleton will try
// to generate an ICU pattern from the skeleton string. Since each locale format
// might have small differences between one another, we try to use a general
// skeleton string to construct a pattern.
icu::UnicodeString GetPatternFromSkeleton(const std::string& locale_id,
                                          const icu::UnicodeString& skeleton) {
  UErrorCode status = U_ZERO_ERROR;
  icu::Locale locale = GetCorrectICULocale(locale_id);

  std::unique_ptr<icu::DateTimePatternGenerator> generator(
      icu::DateTimePatternGenerator::createInstance(locale, status));

  if (U_FAILURE(status)) {
    SB_LOG(WARNING) << "Unable to create DatTimePatternGenerator for "
                       "GetPatternFromSkeleton. GetPatternFromSkeleton will "
                       "return the empty string.";
    return icu::UnicodeString();
  }

  icu::UnicodeString pattern = generator->getBestPattern(skeleton, status);

  if (U_FAILURE(status)) {
    SB_LOG(WARNING) << "getBestPattern failed to generate the best pattern for "
                       "the the skeleton. Returning the empty string.";
    return icu::UnicodeString();
  }

  return pattern;
}
}  // namespace

std::string GetLocalizedDateSymbol(const std::string& locale,
                                   TimeNameType type,
                                   nl_item item) {
  if (locale == cobalt::kCLocale || locale == cobalt::kPosixLocale) {
    return GetCTimeLanginfo(item);
  }
  std::string result;

  UErrorCode status = U_ZERO_ERROR;
  icu::Locale icu_locale = GetCorrectICULocale(locale);

  icu::DateFormatSymbols syms(icu_locale, status);
  if (U_FAILURE(status)) {
    return "";
  }

  int count = 0;
  int index;

  switch (type) {
    case TimeNameType::kDay:
      index = item - DAY_1;
      result =
          ToUtf8(syms.getWeekdays(count, icu::DateFormatSymbols::STANDALONE,
                                  icu::DateFormatSymbols::WIDE)[index + 1]);
      break;
    case TimeNameType::kAbbrevDay:
      index = item - ABDAY_1;
      result = ToUtf8(
          syms.getWeekdays(count, icu::DateFormatSymbols::STANDALONE,
                           icu::DateFormatSymbols::ABBREVIATED)[index + 1]);
      break;
    case TimeNameType::kMonth:
      index = item - MON_1;
      result = ToUtf8(syms.getMonths(count, icu::DateFormatSymbols::STANDALONE,
                                     icu::DateFormatSymbols::WIDE)[index]);
      break;
    case TimeNameType::kAbbrevMonth:
      index = item - ABMON_1;
      result =
          ToUtf8(syms.getMonths(count, icu::DateFormatSymbols::STANDALONE,
                                icu::DateFormatSymbols::ABBREVIATED)[index]);
      break;
    case TimeNameType::kAmPm:
      index = item - AM_STR;
      result = ToUtf8(syms.getAmPmStrings(count)[index]);
  }

  return result;
}

std::string NlGetNumericData(const std::string& locale, nl_item type) {
  if (locale == cobalt::kCLocale || locale == cobalt::kPosixLocale) {
    return GetCNumericLanginfo(type);
  }
  std::string result;

  UErrorCode status = U_ZERO_ERROR;
  icu::Locale icu_locale = GetCorrectICULocale(locale);

  icu::DecimalFormatSymbols syms(icu_locale, status);
  if (U_FAILURE(status)) {
    return "";
  }

  switch (type) {
    case RADIXCHAR:
      result = ToUtf8(
          syms.getSymbol(icu::DecimalFormatSymbols::kDecimalSeparatorSymbol));
      break;
    case THOUSEP:
      result = ToUtf8(
          syms.getSymbol(icu::DecimalFormatSymbols::kGroupingSeparatorSymbol));
      break;
    default:
      SB_LOG(WARNING) << "Unknown nl_item type received for NlGetNumericData. "
                         "Returning the empty string.";
      result = "";
  }

  return result;
}

std::string GetPosixPattern(const std::string& locale, nl_item item) {
  if (locale == cobalt::kCLocale || locale == cobalt::kPosixLocale) {
    return GetCTimeLanginfo(item);
  }

  icu::UnicodeString icu_pattern;

  // For |D_FMT| and |T_FMT|, ICU has stored data for each locale that closely
  // align with what POSIX expects in terms of formatting. This allows us to
  // retrieve an |icu_pattern| that is already stored.
  //
  // For |D_T_FMT| and |T_FMT_AMPM|, we use skeleton strings to construct an
  // |icu_pattern| as ICU does not store a date/time pattern that holds all the
  // information these nl_items expect.
  switch (item) {
    case D_FMT:
      icu_pattern = GetPatternFromStyle(locale, UDAT_SHORT, UDAT_NONE);
      break;
    case T_FMT:
      icu_pattern = GetPatternFromStyle(locale, UDAT_NONE, UDAT_MEDIUM);
      break;
    case D_T_FMT:
      static const icu::UnicodeString kDtPattern(kSkeletonDtFmt);
      icu_pattern = GetPatternFromSkeleton(locale, kDtPattern);
      break;
    case T_FMT_AMPM:
      static const icu::UnicodeString kTAmPmPattern(kSkeletonTFmtAmPm);
      icu_pattern = GetPatternFromSkeleton(locale, kTAmPmPattern);
      break;
    default:
      SB_LOG(WARNING)
          << "GetPosixPattern was called with an unsupported nl_item: " << item
          << ". Returning the empty string.";
      icu_pattern = "";
  }

  std::string converted_pattern = IcuPatternToPosix(icu_pattern);
  return CollapseSpaces(converted_pattern);
}
}  // namespace cobalt
