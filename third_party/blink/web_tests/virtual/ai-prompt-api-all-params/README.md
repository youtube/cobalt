This virtual suite executes AI Prompt API tests with both AIPromptAPIParams
and AIPromptAPILegacyParams flags enabled. This allows testing of legacy raw
parameters and new parameter interfaces simultaneously, matching the Extension
context.
