package dev.cobalt.util.startupguardexceptions;

/**
 * Custom runtime exception for startup-related guard failures.
 */
public class StartupGuardException49 extends RuntimeException {

    // Default constructor
    public StartupGuardException49() {
        super();
    }

    // Constructor that accepts a custom error message
    public StartupGuardException49(String message) {
        super(message);
    }

    // Constructor that accepts a message and a cause (for exception chaining)
    public StartupGuardException49(String message, Throwable cause) {
        super(message, cause);
    }

    // Constructor that accepts only a cause
    public StartupGuardException49(Throwable cause) {
        super(cause);
    }
}
