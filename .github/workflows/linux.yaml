name: linux

on:
  pull_request:
    types: [opened, reopened, edited, synchronize]
    branches: [ main, 23.lts.1+ ]
  push:
    branches: [ main, 23.lts.1+ ]
  workflow_dispatch:

jobs:
  CI:
    uses: ./.github/workflows/main.yaml
    permissions:
      packages: write
    with:
      platform: $GITHUB_WORKFLOW

