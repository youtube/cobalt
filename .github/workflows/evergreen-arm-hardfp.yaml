name: evergreen-arm-hardfp

on:
  pull_request:
    types: [opened, reopened, edited, synchronize]
    branches: [ devel ]
  push:
    branches: [ devel ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/main.yaml
    permissions:
      packages: write
    with:
      platform: $GITHUB_WORKFLOW

