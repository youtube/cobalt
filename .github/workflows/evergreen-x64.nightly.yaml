name: evergreen-x64.nightly

on:
  schedule:
    - cron: '* 21 * * *'
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/main.yaml
    permissions:
      packages: write
    with:
      platform: evergreen-x64
