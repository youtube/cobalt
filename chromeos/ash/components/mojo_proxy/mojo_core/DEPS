# This directory is a frozen, self-contained clone of legacy Mojo Core and
# must not depend on //mojo (see README.md).
include_rules = [
  "-mojo",

  # TODO(crbug.com/359926651): remove in the next CL together with the
  # remaining ipcz code paths.
  "+third_party/ipcz/include",

  # Mirrors //mojo/public/DEPS: simple_watcher.cc emits a typed tracing event.
  "+third_party/perfetto/protos/perfetto/trace/track_event",
]
