source_set("splash") {
  testonly = true

  sources = [
    "splash.cc",
    "splash.h",
  ]

  deps = [
    "//base",
    "//cobalt/shell:cobalt_shell_lib",
    "//content/public/browser",
  ]

  if (is_android) {
    deps += [
      "//starboard/android/shared:starboard_platform",
      "//ui/android",
    ]
  }
}
